[project]
name = "sqlparser"
version = "0.1.0"
description = "Parse T-SQL by named blocks while maintaining dependencies"

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2"]
